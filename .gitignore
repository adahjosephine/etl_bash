# Environment variables / secrets — never commit these
.env

# Generated data — regenerated by running the pipeline, not source-controlled
raw/
Transformed/
Gold/
json_and_CSV/

# OS/editor junk
.DS_Store
*.swp
.vscode/